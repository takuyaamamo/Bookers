<h1>Editing Book</h1>
<%# url:とmethod:を加えることで無理やり指定するmethodは不要かも %>
<%= form_for(@book, url: {action: "update"}, method: :patch) do |f| %>

<h4>Title</h4>
<%=  f.text_field :title %>

<h4>Body</h4>
<%= f.text_area :body %>

<%= f.submit 'Update Book' %>

<% end %>

<%= link_to 'Show', show_path(@book.id) %>
|
<a href="/books">Back</a>
